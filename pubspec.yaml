name: syncbeat
description: Multi-device sync player.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_database: ^11.0.0
  youtube_player_flutter: ^9.0.0

flutter:
  uses-material-design: true
